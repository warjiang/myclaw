# Checklist

- [ ] Project initialized with `pyproject.toml` and dependencies.
- [ ] Core Agent (`ClawAgent`) implemented using `ClaudeSDKClient`.
- [ ] Skills loader implemented (loads `SKILL.md`).
- [ ] Config loader implemented (compatible with nanobot structure).
- [ ] Basic CLI interface functional.
- [ ] Docker container builds successfully.
- [ ] GitHub Workflow configured.
- [ ] Verified tool integration (MCP).
- [ ] Verified skill import.
